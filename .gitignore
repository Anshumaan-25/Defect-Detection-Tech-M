# --- Secrets ---------------------------------------------------------------
.env

# --- Data & model weights (large; sourced/produced, not versioned) --------
data/raw/
data/processed/
models/yolo/*.pt
# YOLO training run dirs (force-add a best.pt with `git add -f` to version one)
models/yolo/*/
models/anomaly/
*.ckpt

# Keep the empty directory structure in git via .gitkeep files.
!data/raw/.gitkeep
!**/.gitkeep

# --- Python ----------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# --- Ultralytics / training run artifacts ---------------------------------
runs/
wandb/

# --- Demo / inference output images (generated, not versioned) -------------
result_*.png
demo_output_*.png

# --- Gradio runtime cache --------------------------------------------------
.gradio/

# --- OS / editor -----------------------------------------------------------
.DS_Store
.vscode/
.idea/
